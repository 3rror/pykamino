#!/usr/bin/env python

from pykamino._cli.commands.scrape import Scrape
from pykamino._cli.dispatcher import Dispatcher

if __name__ == "__main__":
    disp = Dispatcher("LOL")
    disp.add_command(Scrape(description='Test'))
    disp.run()

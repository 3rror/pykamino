[global]
products = [ "BTC-USD" ]

[scraper]
    [scraper.database]
    dbms = "postgres"
    user = "postgres"
    password = "password"
    hostname = "localhost"
    port = "32768"
    db_name = "postgres"

